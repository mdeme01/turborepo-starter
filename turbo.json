{
    "$schema": "https://turbo.build/schema.json",
    "globalEnv": [
        "POSTGRES_URL",
        "POSTGRES_HOST",
        "POSTGRES_PORT",
        "POSTGRES_DB",
        "POSTGRES_USER",
        "POSTGRES_PASSWORD",
        "SERVER_URL",
        "SERVER_HOST",
        "SERVER_PORT",
        "WEB_URL",
        "WEB_HOST",
        "WEB_PORT"
    ],
    "tasks": {
        "build": {
            "dependsOn": ["^build"],
            "inputs": ["$TURBO_DEFAULT$", ".env", ".env.*"]
        },
        "lint": {
            "dependsOn": ["^lint"]
        },
        "type-check": {
            "dependsOn": ["^type-check"]
        },
        "dev": {
            "cache": false,
            "persistent": true
        },
        "migrate": {},
        "seed": {}
    }
}
